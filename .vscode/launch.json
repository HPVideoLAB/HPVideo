{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run FastAPI with start.sh",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/backend/start.sh",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/backend"
      }
    }
  ]
}
